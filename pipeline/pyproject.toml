[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "stillhere-pipeline"
version = "0.0.1"
description = "Still Here SD ingestion, validation, analysis, and export pipeline"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6",
]

[project.scripts]
# Same entry point as `python -m stillhere_pipeline.eyepop_audit`.
stillhere-audit = "stillhere_pipeline.eyepop_audit:main"

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "ruff>=0.6",
    "mypy>=1.10",
    "types-PyYAML>=6",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.mypy]
strict = true
mypy_path = "src"
